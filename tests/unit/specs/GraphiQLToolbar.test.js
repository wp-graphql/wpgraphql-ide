/**
 * @todo should this test live near the component it is testing?
 *
 * related: https://github.com/wp-graphql/wpgraphql-ide/issues/31
 */
describe('GraphiQL Toolbar', () => {

	test('it should render', () => {})
	test('it should render with prettify button', () => {})

	test('filter to add button as the first toolbar button', () => {})
	test('filter to add button as the last toolbar button', () => {})
	test('filter to add button after the prettify button', () => {})
	test('filter to remove prettify button', () => {})
	test('filter remove all buttons except a custom button', () => {})

	// this test is not ported over because the history button is no longer
	// in the toolbar but is over on the left sidebar nav
	test.skip('it should render with history button', () => {})
})
